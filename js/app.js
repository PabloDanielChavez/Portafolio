import { obtenerPlan } from './API.js'; import { mostrarPlan } from './funciones.js'; (function () { document.addEventListener("DOMContentLoaded", cargarDatos); async function cargarDatos() { const planes = await obtenerPlan(); planes.forEach( datos => { const { nombre, info, paginas, revisiones, precio, descuento} = datos; mostrarPlan( nombre, info, paginas, revisiones, precio, descuento ); }); } })();